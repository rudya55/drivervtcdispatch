name: Build Android APK/AAB

on:
  workflow_dispatch:
    inputs:
      build_type:
        description: 'Type de build (debug/release)'
        required: false
        default: 'release'
        type: choice
        options:
          - debug
          - release
  push:
    branches:
      - main
    paths:
      - 'android/**'
      - 'src/**'
      - 'package.json'
      - 'capacitor.config.ts'

jobs:
  build:
    name: Build Android Application
    runs-on: ubuntu-latest
    env:
      NODE_OPTIONS: --max_old_space_size=4096
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Debug list Gradle files
        run: |
          echo "Current working dir: $(pwd)"
          echo "Top-level files:" && ls -la
          echo "Looking for Gradle files:"
          find . -type f \( -name "*.gradle*" -o -name "gradle-wrapper.properties" \) -print || true

      - name: Setup Node.js 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'
          cache: 'gradle'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Build web app (Vite)
        run: npm run build
        env:
          NODE_ENV: production

      - name: Sync Capacitor Android
        run: npx cap sync android

      - name: Grant Gradle execution permission
        working-directory: android
        run: chmod +x ./gradlew

      - name: Install Android SDK cmdline-tools
        shell: bash
        run: |
          ANDROID_SDK_ROOT="$RUNNER_TEMP/android-sdk"
          mkdir -p "$ANDROID_SDK_ROOT/cmdline-tools"
          cd "$RUNNER_TEMP"
          curl -sSLo cmdline-tools.zip https://dl.google.com/android/repository/commandlinetools-linux-9477386_latest.zip
          unzip -q cmdline-tools.zip -d "$ANDROID_SDK_ROOT/cmdline-tools"
          mv "$ANDROID_SDK_ROOT/cmdline-tools/cmdline-tools" "$ANDROID_SDK_ROOT/cmdline-tools/latest" || true
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT" >> "$GITHUB_ENV"
          echo "$ANDROID_SDK_ROOT/cmdline-tools/latest/bin" >> "$GITHUB_PATH"
          echo "$ANDROID_SDK_ROOT/platform-tools" >> "$GITHUB_PATH"

      - name: Accept Android SDK licenses
        run: yes | sdkmanager --licenses

      - name: Install Android SDK packages
        run: sdkmanager "platform-tools" "platforms;android-35" "build-tools;35.0.0"

      - name: Configure local.properties
        run: |
          echo "sdk.dir=$ANDROID_SDK_ROOT" > android/local.properties
          cat android/local.properties

      - name: Decode Keystore
        if: github.event_name != 'pull_request'
        env:
          KEYSTORE_BASE64: ${{ secrets.KEYSTORE_BASE64 }}
        run: |
          if [ ! -z "$KEYSTORE_BASE64" ]; then
            echo "$KEYSTORE_BASE64" | base64 -d > android/app/upload-keystore.jks
            echo "✅ Keystore décodé avec succès"
          else
            echo "⚠️ KEYSTORE_BASE64 non configuré, build sans signature"
          fi

      - name: Create key.properties
        if: github.event_name != 'pull_request'
        env:
          KEYSTORE_PASSWORD: ${{ secrets.KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.KEY_PASSWORD }}
        run: |
          if [ ! -z "$KEYSTORE_PASSWORD" ]; then
            cat > android/key.properties << EOF
          storePassword=$KEYSTORE_PASSWORD
          keyPassword=$KEY_PASSWORD
          keyAlias=$KEY_ALIAS
          storeFile=upload-keystore.jks
          EOF
            echo "✅ key.properties créé"
          else
            echo "⚠️ Secrets de signature non configurés"
          fi

      - name: Build Android APK (Debug)
        if: github.event.inputs.build_type != 'release'
        working-directory: android
        run: |
          ./gradlew assembleDebug --no-daemon --stacktrace --warning-mode all
          echo "✅ Debug APK built successfully"
          ls -lh app/build/outputs/apk/debug/

      - name: Build Android APK (Release)
        if: github.event_name != 'pull_request'
        working-directory: android
        run: |
          if [ -f "key.properties" ]; then
            echo "🔑 Building SIGNED release APK"
            ./gradlew assembleRelease --no-daemon --stacktrace --warning-mode all
            echo "✅ Signed release APK built successfully"
          else
            echo "⚠️ Building UNSIGNED release APK (no key.properties)"
            ./gradlew assembleRelease --no-daemon --stacktrace --warning-mode all
            echo "⚠️ Unsigned release APK built"
          fi
          ls -lh app/build/outputs/apk/release/

      - name: Build Android App Bundle (AAB)
        if: github.event_name != 'pull_request'
        working-directory: android
        run: |
          if [ -f "key.properties" ]; then
            echo "🔑 Building SIGNED AAB for Play Store"
            ./gradlew bundleRelease --no-daemon --stacktrace --warning-mode all
            echo "✅ Signed AAB built successfully"
            ls -lh app/build/outputs/bundle/release/
          else
            echo "⚠️ Skipping AAB build (no key.properties)"
          fi

      - name: Get APK/AAB Info
        id: apk_info
        working-directory: android
        run: |
          if [ -f "app/build/outputs/apk/release/app-release.apk" ]; then
            APK_SIZE=$(du -h app/build/outputs/apk/release/app-release.apk | cut -f1)
            echo "apk_size=$APK_SIZE" >> $GITHUB_OUTPUT
          fi
          if [ -f "app/build/outputs/bundle/release/app-release.aab" ]; then
            AAB_SIZE=$(du -h app/build/outputs/bundle/release/app-release.aab | cut -f1)
            echo "aab_size=$AAB_SIZE" >> $GITHUB_OUTPUT
          fi

      - name: Upload APKs
        uses: actions/upload-artifact@v4
        with:
          name: android-apk-${{ github.sha }}
          path: |
            android/app/build/outputs/apk/**/*.apk
          retention-days: 30

      - name: Upload AAB (Play Store)
        if: github.event_name != 'pull_request'
        uses: actions/upload-artifact@v4
        with:
          name: android-aab-${{ github.sha }}
          path: |
            android/app/build/outputs/bundle/**/*.aab
          retention-days: 90

      - name: Build Summary
        run: |
          echo "## 📦 Build Android terminé avec succès" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📱 Application: VTC Driver Dispatch" >> $GITHUB_STEP_SUMMARY
          echo "- **Package**: com.lovable.drivervtcdispatch" >> $GITHUB_STEP_SUMMARY
          echo "- **Version**: 1.0.0 (Build 1)" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit**: \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f "android/key.properties" ]; then
            echo "### ✅ APK et AAB signés (prêts pour le Play Store)" >> $GITHUB_STEP_SUMMARY
            echo "- **APK signé**: ${{ steps.apk_info.outputs.apk_size }}" >> $GITHUB_STEP_SUMMARY
            echo "- **AAB signé**: ${{ steps.apk_info.outputs.aab_size }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "🎉 Téléchargez l'AAB pour le publier sur le Play Store" >> $GITHUB_STEP_SUMMARY
          else
            echo "### ⚠️ APK non signé (mode test uniquement)" >> $GITHUB_STEP_SUMMARY
            echo "- **APK debug**: ${{ steps.apk_info.outputs.apk_size }}" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "ℹ️ Configurez les secrets GitHub pour générer un APK/AAB signé:" >> $GITHUB_STEP_SUMMARY
            echo "- \`KEYSTORE_BASE64\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`KEYSTORE_PASSWORD\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`KEY_ALIAS\`" >> $GITHUB_STEP_SUMMARY
            echo "- \`KEY_PASSWORD\`" >> $GITHUB_STEP_SUMMARY
          fi
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "📥 **Télécharger les fichiers** dans l'onglet 'Artifacts' ci-dessus" >> $GITHUB_STEP_SUMMARY
